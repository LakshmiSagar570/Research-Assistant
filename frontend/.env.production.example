# Used by `vite build` when deploying to production (Vercel picks this up
# automatically for production builds). Replace with your actual deployed
# backend URL from DEPLOYMENT.md step 2.
VITE_API_URL=https://YOUR-BACKEND-URL.vercel.app
